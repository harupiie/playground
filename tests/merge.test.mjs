import { describe, it } from 'node:test';

// フェーズ1: アウトライン（it.todo のみ）

describe('applyUpdates', () => {
  describe('再取得した記事情報で既存データを最新化できること', () => {
    it.todo('既存記事のタイトルが新しい取得結果で上書きされること');
    it.todo('既存記事の日付が新しい取得結果で上書きされること');
    it.todo('新しい取得結果のタイトルが空の時、既存のタイトルが維持されること');
    it.todo('新しい取得結果の日付が null の時、既存の日付が維持されること');
  });

  describe('呼び出し元のデータに影響を与えないこと', () => {
    it.todo('渡した Map が変更されないこと');
  });

  describe('新規 URL の追加は呼び出し側が担うこと', () => {
    it.todo('既存マップに存在しない URL の記事は追加されないこと');
  });
});

describe('filterExpired', () => {
  describe('表示対象の記事のみを残せること', () => {
    it.todo('基準日時より新しい記事は残ること');
    it.todo('基準日時より古い記事は除外されること');
    it.todo('日付が取得できなかった記事は有効な記事として残ること');
  });

  describe('呼び出し元のデータに影響を与えないこと', () => {
    it.todo('渡した配列が変更されないこと');
  });
});

describe('sortByDate', () => {
  describe('新着順に記事を並べられること', () => {
    it.todo('日付が新しい記事が先頭になること');
    it.todo('日付が取得できなかった記事は日付明確な記事の後ろに表示されること');
    it.todo('日付が同じ記事の並び順が一定であること');
  });

  describe('呼び出し元のデータに影響を与えないこと', () => {
    it.todo('渡した配列が変更されないこと');
  });
});
